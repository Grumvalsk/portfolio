import { DynamicZoomDirective } from './dynamic-zoom.directive';

describe('DynamicZoomDirective', () => {
  it('should create an instance', () => {
    const directive = new DynamicZoomDirective();
    expect(directive).toBeTruthy();
  });
});
